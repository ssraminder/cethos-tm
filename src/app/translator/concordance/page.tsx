import { PageHeader, PlaceholderCard } from "@/components/AppShell";
export default function ConcordancePage() {
  return (
    <>
      <PageHeader title="Concordance" subtitle="Search prior translations in your assigned TMs." />
      <PlaceholderCard title="Concordance search" body="Available once a job is open in the editor." />
    </>
  );
}
